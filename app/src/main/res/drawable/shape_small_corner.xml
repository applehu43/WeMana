<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 设置透明背景色 -->
    <solid android:color="@color/back_orange" />
    <!-- 渐变 -->
    <gradient
        android:angle="270"
        android:endColor="@color/back_orange"
        android:startColor="@color/light_green" />

    <!-- 设置一个黑色边框 -->
    <stroke
        android:width="2px"
        android:color="@color/back_white" />
    <!-- 设置四个圆角的半径 -->
    <!--<corners-->
        <!--android:bottomLeftRadius="3px"-->
        <!--android:bottomRightRadius="3px"-->
        <!--android:topLeftRadius="3px"-->
        <!--android:topRightRadius="3px" />-->
    <!-- 设置一下边距,让空间大一点 -->
    <padding
        android:bottom="5dp"
        android:left="5dp"
        android:right="5dp"
        android:top="5dp" />

</shape>