<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chaohu.wemana">

    <!--original logo android:icon="@mipmap/ic_launcher"-->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_test1"

        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity
            android:name=".activities.HomeActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"></action>
                <category android:name="android.intent.category.LAUNCHER"></category>
            </intent-filter>
        </activity>
        <activity android:name=".activities.SimpleChartDemo"></activity>

        <!-- 定义Android应用的一个组件 -->
        <!--<activity-->
        <!--android:name=".activities.RecordWeightActivity"-->
        <!--android:label="@string/app_name" >-->
        <!--<intent-filter>-->
        <!--&lt;!&ndash;&lt;!&ndash; 指定该Activity是程序的入口&ndash;&gt;&ndash;&gt;-->
        <!--<action android:name="android.intent.action.MAIN" />-->
        <!--&lt;!&ndash;&lt;!&ndash; 指定加载该应用时运行该Activity&ndash;&gt;&ndash;&gt;-->
        <!--<category android:name="android.intent.category.LAUNCHER" />-->
        <!--</intent-filter>-->
        <!--</activity>-->
        <!--<activity android:name=".activities.HomeActivity">-->
        <!--</activity>-->
    </application>

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"></uses-permission>
    <!-- 在SDCard中创建与删除文件权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"></uses-permission>
    <!-- 往SDCard写入数据权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>

</manifest>
